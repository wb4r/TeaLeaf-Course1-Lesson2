# It is so because of the self.class. If it would say self.name,
# in case there was an instance variable for name, it would
# display it but since it points to the class, and the obj where
# we are activating it is car1 which is class Car it displays it